/**
 * when we declare any type of Arry-
 * syntax: datatype[]
 */
const myNames = ['Razu', 'rakib', 77, ,null];

const names: string[] = ['Razu', 'rakib', 'rajib', 'sakib'];

const numbers: number[] = [10, 2, 1, 25, 16];

// names.map((value) => {
//     console.log(value);
// })


/**
 * tuple
 * const variable_name: [type,type]= [value, value]
 */

// let user: [number, string] = [10, 'Razu']
// // user[0] = "abc"; // error
// user[0] = 12;


const isMarid: [string, boolean] = ["Razu", false];
